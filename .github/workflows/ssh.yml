name: bashshell
on: [push]
jobs:
  build:
    runs-on: ubuntu-18.04
    
    steps:
 #   - name: 打印ip
  #    run: |
   #        ip addr
    - name: Download staging_dir
      env: 
       DL_URL: https://image.moeclub.org/GoogleDrive/
       DL_URL1: 1YUsSrNBXq0fDXDnqFsG70Am75FREL32F
       DL_NAME: staging_dir.tar.gz
       REPO_NAME: openwrt 
      run: | 
        mkdir $REPO_NAME && cd $REPO_NAME
        wget  $DL_URL/$DL_URL1
        mv $DL_URL1 $DL_NAME
        tar -xzvf $DL_NAME
        sudo rm -rf $DL_NAME
        ls -la
    #- name: SSH connection to Actions
     # uses: p3terx/debugger-action@master  

