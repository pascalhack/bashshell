name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 打印ip
      run: |
           ip addr
    - name: SSH connection to Actions
      uses: p3terx/debugger-action@master  

